name: Automatsko slanje emaila

on:
  schedule:
    - cron: "*/10 * * * *"  # Pokreće se svakih 10 minuta
  workflow_dispatch:  # Omogućuje ručno pokretanje workflow-a

jobs:
  send_email:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Kloniranje repozitorija
        uses: actions/checkout@v4

      - name: 🔧 Postavljanje Python okruženja
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: 📦 Instalacija ovisnosti
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🔑 Postavljanje okolišnih varijabli
        run: |
          echo "TOKEN_FILE=token.pickle" >> $GITHUB_ENV
          echo "CLIENT_SECRET_FILE=client_secret.json" >> $GITHUB_ENV

      - name: 🚀 Pokretanje skripte
        run: python my_script.py
